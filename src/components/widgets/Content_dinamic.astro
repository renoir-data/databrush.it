---
import type { Content as Props } from '~/types';
import Headline from '../ui/Headline.astro';
import WidgetWrapper from '../ui/WidgetWrapper.astro';
import Button from '~/components/ui/Button.astro';
import ItemGrid from '../ui/ItemGrid.astro';
import company from '../../assets/images/use_cases/case1.jpg';
import company_dark from '../../assets/images/use_cases/case1_dark.jpg';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  content = await Astro.slots.render('content'),
  callToAction,
  items = [],
  columns,
  isReversed = false,
  isAfterContent = false,
  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;
---

<WidgetWrapper
  id={id}
  isDark={isDark}
  containerClass={`max-w-7xl mx-auto ${isAfterContent ? 'pt-0 md:pt-0 lg:pt-0' : ''} ${classes?.container ?? ''}`}
  bg={bg}
>
  <Headline
    title={title}
    subtitle={subtitle}
    tagline={tagline}
    classes={{
      container: 'max-w-xl sm:mx-auto lg:max-w-2xl',
      title: 'text-4xl md:text-5xl font-bold tracking-tighter mb-4 font-heading',
      subtitle: 'max-w-3xl mx-auto sm:text-center text-xl text-muted dark:text-slate-400',
    }}
  />
  <div class="mx-auto max-w-7xl p-4 md:px-8">
    <div class={`md:flex ${isReversed ? 'md:flex-row-reverse' : ''} md:gap-16`}>
      <div class="md:basis-1/2 self-center">
        {content && <div class="mb-12 text-lg dark:text-slate-400" set:html={content} />}
        {
          callToAction && (
            <div class="mt-[-40px] mb-8 text-primary">
              <Button variant="link" {...callToAction} />
            </div>
          )
        }
        <ItemGrid
          items={items}
          columns={columns}
          defaultIcon="tabler:check"
          classes={{
            container: `gap-y-2 md:gap-y-4`,
            panel: 'max-w-none hover:ring ring-primary-500/70 transition-shadow duration-300 rounded-lg p-4 hover-div',
            title: 'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2 md:max-xl:text-2xl',
            description: 'text-muted dark:text-slate-400 ml-2 rtl:ml-0 rtl:mr-2 md:max-xl:hidden',
            icon: 'flex h-7 w-7 items-center justify-center rounded-full bg-green-600 dark:bg-green-700 text-gray-50 p-1',
            action: 'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
          }}
        />
      </div>
      <div aria-hidden="true" class="mt-10 md:mt-0 md:basis-1/2">
        <div class="relative m-auto max-w-4xl dark:hidden">
          <img id="slideshow_light" src={company.src} class="w-full h-auto rounded-3xl " alt="team" />
        </div>
        <div class="relative m-auto max-w-4xl hidden dark:block">
          <img id="slideshow_dark" src={company_dark.src} class="w-full h-auto rounded-3xl " alt="team" />
        </div>
      </div>
    </div>
  </div>
</WidgetWrapper>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  .fade {
    animation: fadeIn 1s ease;
  }

  @keyframes fadeOut {
    from {
      opacity: 1;
    }
    to {
      opacity: 0;
    }
  }
  .fade-out {
    animation: fadeOut 0.5s ease;
  }
</style>

<script>
  import company from '~/assets/images/use_cases/case1.jpg';
  import processes from '~/assets/images/use_cases/case2.jpg';
  import value from '~/assets/images/use_cases/case3.jpg';

  import company_dark from '~/assets/images/use_cases/case1_dark.jpg';
  import processes_dark from '~/assets/images/use_cases/case2_dark.jpg';
  import value_dark from '~/assets/images/use_cases/case3_dark.jpg';

  document.addEventListener("DOMContentLoaded", () => {
    // Array di immagini (usa .src per ottenere il percorso)
    const images_light = [
      company.src,
      processes.src,
      value.src,
    ];

    const images_dark = [
      company_dark.src,
      processes_dark.src,
      value_dark.src,
    ];

    let currentIndex = 0;
    let interval;
    const slideshow_light = document.getElementById("slideshow_light");
    const slideshow_dark = document.getElementById("slideshow_dark");

    /**
     * Cambia immagine con animazione:
     * - Applica il fade-out alla vecchia immagine.
     * - Al termine del fade-out, cambia l'immagine e applica il fade-in.
     *
     * @param {number} index - L'indice della nuova immagine.
     */
    function changeImage(index) {
      // Handle light theme slideshow
      if (slideshow_light) {
        slideshow_light.classList.remove("fade");
        slideshow_light.classList.add("fade-out");

        slideshow_light.addEventListener("animationend", function handleFadeOut(e) {
          if (e.animationName !== "fadeOut") return;
          slideshow_light.classList.remove("fade-out");

          (slideshow_light as HTMLImageElement).src = images_light[index];

          void slideshow_light.offsetWidth;

          slideshow_light.classList.add("fade");

          slideshow_light.addEventListener("animationend", function handleFadeIn(e) {
            if (e.animationName !== "fadeIn") return;
            slideshow_light.classList.remove("fade");
          }, { once: true });
        }, { once: true });
      }

      // Handle dark theme slideshow
      if (slideshow_dark) {
        slideshow_dark.classList.remove("fade");
        slideshow_dark.classList.add("fade-out");

        slideshow_dark.addEventListener("animationend", function handleFadeOut(e) {
          if (e.animationName !== "fadeOut") return;
          slideshow_dark.classList.remove("fade-out");

          (slideshow_dark as HTMLImageElement).src = images_dark[index];
          currentIndex = index;

          void slideshow_dark.offsetWidth;

          slideshow_dark.classList.add("fade");

          slideshow_dark.addEventListener("animationend", function handleFadeIn(e) {
            if (e.animationName !== "fadeIn") return;
            slideshow_dark.classList.remove("fade");
          }, { once: true });
        }, { once: true });
      }
    }

    /**
     * Avvia lo slideshow automaticamente.
     * L'intervallo è impostato a 4000ms per dare tempo al fade-out (2s) e al fade-in (2s).
     */
    function startSlideshow() {
      clearInterval(interval);
      interval = setInterval(() => {
        const nextIndex = (currentIndex + 1) % 3;
        changeImage(nextIndex);
      }, 4000);
    }

    /**
     * Ferma lo slideshow.
     */
    function stopSlideshow() {
      clearInterval(interval);
    }

    // Avvio dello slideshow all'entrata nel DOM
    startSlideshow();

    // Aggiungo gli event listener per i div con la classe "hover-div"
    document.querySelectorAll(".hover-div").forEach((div, index) => {
      div.addEventListener("mouseenter", () => {
        stopSlideshow();
        changeImage(index);
      });
      div.addEventListener("mouseover", () => {
        stopSlideshow();
      });
      div.addEventListener("mouseout", () => {
        startSlideshow();
      });
    });
  });
</script>
